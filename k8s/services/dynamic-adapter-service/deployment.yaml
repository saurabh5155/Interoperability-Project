apiVersion: apps/v1
kind: Deployment
metadata:
  name: dynamic-adapter-service
  namespace: interop
  labels:
    app: dynamic-adapter-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dynamic-adapter-service
  template:
    metadata:
      labels:
        app: dynamic-adapter-service
        tier: backend
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: dynamic-adapter-service
          image: ghcr.io/your-org/dynamic-adapter-service:latest
          ports:
            - name: http
              containerPort: 8084
            - name: management
              containerPort: 9090
          envFrom:
            - configMapRef:
                name: interop-config
            - secretRef:
                name: interop-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "750m"
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 9090
            initialDelaySeconds: 30
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 9090
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            failureThreshold: 12
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: dynamic-adapter-service
  namespace: interop
spec:
  selector:
    app: dynamic-adapter-service
  ports:
    - port: 8084
      targetPort: 8084
